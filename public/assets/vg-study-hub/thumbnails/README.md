# YouTube Thumbnails

Upload VG Study Hub YouTube thumbnail `.webp` images in this folder.

Public URL format:

`/assets/vg-study-hub/thumbnails/file-name.webp`

Add each new image to the `thumbnails` array in `components/VGStudyHubShowcase.tsx`.
