# Posters

Upload VG Study Hub poster `.webp` images in this folder.

Public URL format:

`/assets/vg-study-hub/posters/file-name.webp`

Add each new image to the `posters` array in `components/VGStudyHubShowcase.tsx`.
