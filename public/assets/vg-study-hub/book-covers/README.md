# Book Covers

Upload VG Study Hub book cover `.webp` images in this folder.

Public URL format:

`/assets/vg-study-hub/book-covers/file-name.webp`

Add each new image to the `covers` array in `components/VGStudyHubShowcase.tsx`.
